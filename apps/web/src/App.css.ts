import {style} from "@vanilla-extract/css";
export const errorMessageStyles = style({
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    width: "100%",
    paddingTop: "10rem"
});